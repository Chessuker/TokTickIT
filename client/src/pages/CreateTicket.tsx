/**
 * Placeholder for the Create Ticket form (Issue #4). Registered now so the
 * shell navigation described in ui-spec.md §3.0 is complete.
 */
function CreateTicket() {
  return (
    <section>
      <h1 className="zg-title">Create Ticket</h1>
      <p className="zg-subtitle">Raise a new ticket as the selected requester.</p>
      <div className="zg-card">
        <p className="zg-muted mb-0">The ticket creation form arrives with Issue #4.</p>
      </div>
    </section>
  )
}

export default CreateTicket
