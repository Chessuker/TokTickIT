PORT=5000
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/toktickit?schema=public"
# Browser origin allowed to send the session cookie (CORS with credentials).
CLIENT_ORIGIN=http://localhost:5173
